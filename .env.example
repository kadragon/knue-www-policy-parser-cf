# Local Development Environment Variables
# Copy this file to .env.local and update values for your environment

# ============================================================================
# GitHub Repository Configuration (v3.0.0 - NEW)
# ============================================================================
# These are set in wrangler.jsonc as 'vars' for production deployment
# For local development, define them in wrangler.toml or .env.local

# GitHub repository in "owner/repo" format
# Example: kadragon/knue-policy-hub
GITHUB_REPO=kadragon/knue-policy-hub

# Git branch to track for policy changes
# Example: main
GITHUB_BRANCH=main

# Optional: GitHub personal access token for authenticated requests
# Increases API rate limit from 60 to 5000 requests per hour
# Note: Do NOT commit this to version control
# GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ============================================================================
# Cloudflare Bindings
# ============================================================================
# These are automatically bound in wrangler.jsonc for production
# For local testing via wrangler dev, configure in wrangler.jsonc

# R2 bucket name for policy storage
R2_BUCKET_NAME=knue-vectorstore

# ============================================================================
# Deprecated Variables (v2.0.0 - Will be removed after 90-day transition)
# ============================================================================
# These are no longer used in v3.0.0 (GitHub-based sync)
# Kept for reference during migration period

# RSS_FEED_BASE_URL=https://www.knue.ac.kr/rssBbsNtt.do
# BOARD_IDS=25,26,11,207,28,256
# PREVIEW_PARSER_BASE_URL=https://knue-www-preview-parser-cf.kangdongouk.workers.dev/
